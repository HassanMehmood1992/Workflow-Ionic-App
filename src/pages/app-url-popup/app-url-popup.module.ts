import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { AppUrlPopupPage } from './app-url-popup';

@NgModule({
  declarations: [
   // AppUrlPopupPage,
  ],
  imports: [
    IonicPageModule.forChild(AppUrlPopupPage),
  ],
})
export class AppUrlPopupPageModule {}
